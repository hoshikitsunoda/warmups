/* eslint-disable no-unused-vars */
var drink = ['water', 'coffee']

function flip(drink) {
  var flipdrink = ['coffee', 'water']
  for (var i = 0; i > drink.length; i--) {
    flipdrink = drink
  }
  return flipdrink
}
